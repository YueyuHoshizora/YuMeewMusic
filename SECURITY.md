# 安全政策

感謝你協助維護 YuMeew Music Studio 與使用者的安全。若你發現可能的安全問題，請依照本文件私下通報，避免在修正完成前公開漏洞細節。

## 支援版本

YuMeew 是持續更新的靜態網站，目前只支援正式網站與 `main` 分支的最新版本。

| 版本 | 安全更新 |
| --- | --- |
| [正式網站](https://ezmusic.yustellar.idv.tw/)／`main` 最新版本 | 支援 |
| 舊提交、分支、Fork、自行修改或第三方重新部署版本 | 不支援 |

安全修正通常直接發布至最新版本，不保證為舊版本提供回溯修正。

## 通報安全問題

請透過 GitHub 的 [私密安全性通報](https://github.com/YueyuHoshizora/YuMeewMusic/security/advisories/new) 提交問題。請勿建立公開 Issue、Discussion 或 Pull Request 揭露尚未修正的漏洞。

建議在通報中包含：

- 問題類型與可能影響。
- 發生問題的頁面、功能、檔案或提交版本。
- 可穩定重現的操作步驟。
- 使用的瀏覽器、瀏覽器版本與作業系統。
- 概念驗證、畫面紀錄或錯誤訊息；請移除 API KEY、權杖及個人資料。
- 你建議的修正方式，以及是否已在其他地方揭露。

若 GitHub 無法提供私密通報入口，請建立不含技術細節的公開 Issue，僅說明需要與維護者私下聯絡。維護者建立私密聯絡管道前，請勿附上漏洞內容、密鑰或可利用的程式碼。

## 處理流程

維護者會盡力依照以下目標處理：

1. 7 天內確認收到通報。
2. 14 天內完成初步影響與重現評估。
3. 依嚴重程度安排修正、測試及發布。
4. 修正發布後，視需要建立 GitHub Security Advisory、CVE 或公開說明。

實際修正時間會受到問題複雜度、第三方服務及瀏覽器行為影響。處理期間可能會向通報者索取補充資料。若問題不在本專案範圍內，也會盡可能說明原因與適合的通報對象。

## 範圍

適合通報的問題包括但不限於：

- 可在 `ezmusic.yustellar.idv.tw` 執行未授權程式碼的 XSS 或內容注入。
- 本不應離開瀏覽器的本機媒體、草稿、人物資料或模型資料遭到傳送或外洩。
- API KEY、存取權杖、部署密鑰或其他敏感資料意外出現在原始碼、建置成品、紀錄或網路請求中。
- IndexedDB、`localStorage`、Cache Storage 或 Service Worker 的跨使用者、跨來源或非預期存取。
- 檔案解析、專案匯入、字幕、圖片、音訊或影片處理可造成程式碼執行、持久化注入或非預期檔案存取。
- PWA 更新或快取機制允許供應鏈置換、長期保留已撤回的惡意程式碼，或繞過正常更新流程。
- 對第三方生成、字幕、Suno 解析或資源代理服務的請求，可繞過前端預期的來源、參數或權限界線。
- 專案依賴或 GitHub Actions 工作流程中的可利用供應鏈風險。

以下情況通常不視為本專案漏洞：

- 需要先控制使用者裝置、瀏覽器設定檔、開發者工具或作業系統的攻擊。
- 使用者自行貼上、執行或安裝不受信任的程式碼、擴充功能或修改版本。
- 已在介面及 README 說明的資料傳送，例如 AI 生成所需的題詞、引用資源、API KEY，或字幕辨識所需的人聲音訊。
- 瀏覽器清除、逐出或限制 IndexedDB、Cache Storage 和 `localStorage` 所造成的資料遺失。
- 單純缺少安全標頭、版本資訊、最佳實務或自動掃描警告，但沒有可說明的實際攻擊途徑與影響。
- 僅影響已停止支援的瀏覽器、舊提交、Fork 或非官方部署。
- 社交工程、網路釣魚、垃圾訊息、流量壓力測試、服務阻斷，或刻意消耗第三方付費額度。

`model-proxy`、`flux-klein`、`lyrics-transcriber` 與 `storyboard-checker` 是各自獨立的 Git 倉庫與部署服務。若問題只存在於其中一個服務，請優先向對應倉庫私密通報；若問題源於 YuMeew 前端與服務的整合方式，則可在本倉庫通報。

## 安全測試規則

進行測試時，請遵守以下原則：

- 只使用你擁有或已獲明確授權的帳戶、API KEY、檔案與資料。
- 優先在本機開發環境或自行部署的版本驗證。
- 不存取、修改、刪除或下載其他人的資料。
- 不上傳惡意內容至第三方服務，不繞過額度、計費、內容政策或存取限制。
- 不執行自動化流量壓力測試、阻斷服務、資源耗盡或會明顯增加營運成本的測試。
- 證明問題存在後立即停止，保留最小且可重現的測試資料。

在遵守本政策、出於善意且避免傷害使用者與服務的前提下，維護者不會因安全研究本身追究通報者。這項說明不代表第三方平台或服務的授權，測試第三方系統前仍須遵守其政策並取得必要許可。

## 專案安全模型

YuMeew 的主要編輯、媒體處理與部分 AI 推論在瀏覽器中完成。部分功能會依使用者操作連線至第三方或專案 Worker；實際傳送內容記載於 [README 的「檔案與隱私」章節](README.md#檔案與隱私)。

使用者輸入的 API KEY 儲存在同一來源的 `localStorage`。這能避免將金鑰寫入專案伺服器，但不能抵抗已取得同源 JavaScript 執行能力、瀏覽器設定檔存取權或裝置控制權的攻擊者。安全報告應區分此既有設計與未預期的金鑰傳送、紀錄或暴露。

## 協調揭露

請在維護者確認修正已發布，或雙方約定的揭露日期到達後，再公開漏洞細節。維護者會在適當情況下於安全公告中感謝通報者；若希望匿名或使用特定名稱，請在通報時註明。

